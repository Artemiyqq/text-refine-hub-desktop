<Page
    x:Class="TextRefineHubDesktop.Views.TextProcessingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Paste your text below" FontSize="20" HorizontalAlignment="Left" Margin="0,0,0,0" Grid.Row="1"/>

        <TextBox Width="500" Grid.Row="2" Margin="0,0,0,50"/>

        <Button Content="Process" Grid.Row="2" Margin="0,250,0,0" VerticalAlignment="Top" RenderTransformOrigin="2.015,0.615" HorizontalAlignment="Center"/>
    </Grid>

</Page>
